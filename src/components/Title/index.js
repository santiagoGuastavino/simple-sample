import React from 'react'

export default function Title ({ customStyles }) {
  return (
    <h1 className={`w-full text-3xl font-bold text-[#00df9a] ${customStyles}`}>
      Simple Sample.
    </h1>
  )
}
